---
name: event-description
description: Составление по материалам источника (сайт организатора, Timepad) описания научно-популярного события, которого ещё нет на elementy.ru: текст с HTML-разметкой как на сайте + материал для формальных полей админки + (при необходимости) описание автора. Использовать по запросу «описание по источнику», «составить описание события», «новое событие», «сделать анонс события».
body: |
  # Описание события из источника (нет на «Элементах»)

  Задача: по описанию события на сайте организатора или на Timepad собрать
  полный «паспорт» события для «Научного календаря». События нет на
  elementy.ru, поэтому работа идёт по исходным материалам источника.

  В отличие от парсинга «Элементов», описание здесь создаётся С ИСПОЛЬЗОВАНИЕМ
  ИИ (не алгоритмически): пользователь даёт текст/ссылки источника, мы по ним
  пишем описание, следуя формату ниже. Правила и примеры накапливаются
  в этом скилле по мере обучения.

  ## 0. Порядок работы

  1. Пользователь присылает источник (ссылка или скопированный текст — сайт
     организатора, Timepad, страница ВК и т.п.). В дальнейшем нужно будет
     собирать новые события с самих источников.
  2. Если есть ссылка — открыть её и взять данные.
  3. Собрать «формальные поля» (раздел 2) — отдельным блоком, готовым
     к вставке в админку.
  4. Написать текст описания события (раздел 3) с HTML-разметкой как на сайте.
  5. Если автора события в данных «Научного календаря» ещё нет — подготовить
     описание автора (раздел 4).
  6. Вывести результат в чат (раздел 5): формальные поля, текст с разметкой
     и HTML-код описания.
  7. Спорные места (новое поле, неоднозначность) — спросить пользователя,
     потом зафиксировать решение в этом скилле.

  ## 1. Источники

  - Сайт организатора (страница события) — основной источник; иногда может
    быть несколько страниц.
  - Timepad — иногда единственный источник, иногда дополнительный, часто 
    именно здесь можно найти ссылку на регистрацию.
  - Если данные в источниках расходятся — приоритет у сайта организатора;
    расхождение показать пользователю и спросить.

  ## 1а. УНИВЕРСАЛЬНЫЙ шаблон ответа (все лектории)

  Шаблон ответа (формальные поля → текст → HTML, разделы 2–4) ОДИН для всех
  лекториев, независимо от того, где беру событие. Нюансы бывают ТОЛЬКО в
  разборе источника (разд. 1б), но описание события формируется одинаково. 
  «Онлайн», nbsp, ёлочки, KLBLOCK — обрабатываются одинаково везде.

  ## 1б. Нюансы разбора источника — по лекториям

  Различия между лекториями затрагивают ТОЛЬКО то, откуда берём данные и
  какие поля есть (формальные поля и структура ответа — единые, см. 1а):

  - **«Живой город» (Тверь):**
    - по умолчанию, если не сказано точнее, место: Тверь, 
      лекторий «Живой город» — из шаблона;
    - почти все данные видны в тексте страницы, ничего не спрятано.
  - **«Прямая речь» (Москва или другие города):**
    - если в качестве адреса указано «Прямая речь», это надо рассматривать
      как площадку, на которой проводится лекция (но могут быть и другие); 
    - событие может проходить и офлайн, и в ОНЛАЙНе (трансляция) — место
      в шапке пишем ОБА: «…, Москва, «Прямая&nbsp;речь» и&nbsp;ОНЛАЙН»;
      онлайн отдельной строкой НЕ выносим — шапка и есть «где проходит»;
    - если есть трансляция, цена двойная: офлайн-билет и онлайн-трансляция — 
      выводим ОБЕ;
    - часть данных в видимом тексте отсутствует и спрятана в мета-полях
      страницы (og:title / og:description / og:url, цена — в JSON-LD/microdata):
      перед прототипом вытащить эти поля из HTML источника и сверить с тем,
      что видно на странице; расхождение показать, но включить то, что
      видно на странице.
    - АДРЕС (улица, дом) в текст описания и тем более в его шапку над
      блоком об авторе НЕ вносим. Место адреса — ТОЛЬКО поле «Место»
      формальных полей: «Москва, Ермолаевский&nbsp;переулок,&nbsp;25,
      «Прямая&nbsp;речь» и&nbsp;ОНЛАЙН». 
    - Низ описания (адрес, проезд, телефон и т.п.) пока НЕ добавляем —
      как его составлять, ещё обсуждаем (см. историю 20.09.2026).
  - **Другие лектории** — добавляем сюда по мере появления; шаблон ответа
    при этом НЕ меняем.

  ## 2. Формальные поля (админка)

  Выводятся ОТДЕЛЬНЫМ блоком, БЕЗ номеров и названий полей, но в строгом
  порядке. Блок готов к вставке в админку: с `&nbsp;`, `<br>` и т.п.

  Порядок полей (закреплён на прототипе 19.09.2026):
  1. Полный с подзаголовком: подзаголовок(лекторий, цикл и т.п., тип (лекция 
     и т.п.), автор, название (в кавычках);
  2. Полный без подзаголовка;
  3. Только название лекции (без кавычек);
  4. Подзаголовок (лекторий/организатор/цикл лекций);
  5. Дата и время;
  6. Место;
  7. Источники (URL);
  8. URL регистрации/покупки билета;
  9. Стоимость;
  10. Возрастные ограничения.

  Пример (лекция «Нейробиология боли», lectory2.ru):

  ```
  Лекторий «Живой город»<br>Лекция Владимира Алипова «Нейробиология боли»
  Лекция Владимира Алипова «Нейробиология боли»
  Нейробиология боли
  Лекторий «Живой город»
  Суббота, 19&nbsp;сентября 2026&nbsp;года, 18:00
  Тверь
  https://lectory2.ru/#lecture_967
  https://lectory2.ru/#lecture_967
  1400&nbsp;руб.
  16+
  ```

  В поле 8: если кнопка «Купить билет» открывает модальное окно на той же
  странице — URL совпадает с источником (поле 7).

  Факт в формальное поле заносится ТОЛЬКО если он подтверждается источником.
  Если поле неизвестно — писать «—» и не выдумывать.

  ## 3. Текст описания и HTML-разметка (как на сайте)

  В выводимый HTML-фрагмент обвязка НЕ включается: ни
  `<div class='itemblock memo'>`, ни сепаратор `<div class='cl'></div>` —
  только внутренние блоки (шапка, KLBLOCK, blockquote, при необходимости
  цена/ссылки).

  Структура описания:

  1. **Шапка, абзац 1** — формальная строка:
     `<p class="small"><b>Суббота, 19&nbsp;сентября 2026&nbsp;года, 18:00, Тверь.</b></p>`
     - день недели (с большой буквы), дата «ДД месяц ГГГГ года»;
     - время начала; время окончания ставим только если известно явно
       или косвенно (указана продолжительность);
     - место: город (и площадка, если известна) или ОНЛАЙН; если есть и
       очное мероприятие, и онлайн, добавляем после места: и ОНЛАЙН.
     - в конце точка.
     Класс — `small` (нижний регистр; вариант `Small` тоже допустим).
  2. **Шапка, абзац 2** — тип мероприятия обычным шрифтом, автор
     в родительном падеже жирным курсивом, название в «ёлочках» жирным,
     в конце точка:
     `<p>Лекция <b><i>Владимира Алипова</i> «Нейробиология боли»</b>.</p>`
  3. **Блок об авторе** — заглушка, которую подставляет система:
     `<KLBLOCK eltclub_authors_about/>`
     НЕ НУЖНО вставлять на этом месте код реального блока, только эта заглушка!
  4. **Аннотация** `<blockquote class="small">…</blockquote>` по материалам
     источника:
     - абзацы источника НЕ переставляем: описание меняем только если для этого
       есть важные причины;
     - Упоминания автора: описание автора — курсивом, имя автора —
        жирным курсивом:
        `<p><i>Нейробиолог <b>Владимир Алипов</b></i> расскажет:</p>`
        Лишнее форматирование источника убираем (жирный заголовок абзаца,
        `<strong>` и т.п.) — наше форматирование должно оставаться
        стандартным; исключение — выделенные в источнике пункты перечня
        (как в лекции Никитина), их можно оставить;
     - перечень тем, о которых расскажут, и другие списки — ненумерованным списком
       `<ul><li>…</li>…</ul>`;
     - явные опечатки источника исправляем и помечаем комментарием
       под прототипом страницы.
  5. **Цена и ссылки на билеты** (по образцу «Элементов»; в прототипе
     «Нейробиологии боли» не подключались — обсудить отдельно):
     `<p>Стоимость билета: <b>1300&nbsp;руб.</b> <a href="…" target=_blank>Купить билет</a>.</p>`
     Если есть ОНЛАЙН или льготные цены (например, для студентов), указываем
     все варианты цены. Если указан диапазон цен, указываем нижнюю границу:
     от 2000 руб.

  ПРАВИЛО СООТВЕТСТВИЯ: сначала готовим HTML-код, потом в тексте для показа
  выводим его с точностью до согласованных изменений (у абзацев, которые 
  предназначены для помещения в однострочные поля, убираем теги абзаца).

  При написании текста применяются корректорские правила из скилла
  `proofreading` (ё, неразбиваемые пробелы, кавычки, тире).

  ВАЖНО: текст описания НЕ сокращаем и НЕ переписываем без надобности.
  Переносим из источника(ов) ВСЁ существенное: все абзацы, все пункты
  списков, подзаголовки, формулировки. Ничего из источника не теряем.
  Допустимо только то, что прямо предписывают правила проекта
  (например, включить подзаголовок в начало текста) — и тоже без потери
  смысла.

  У АВТОРА два описания, оба НЕ теряем:
  1. **Абзац об авторе из источника** — это СОДЕРЖАТЕЛЬНЫЙ текст (в нём
     факты, регалии, чем занимается). Переносим его в описание лекции
     (внутри `blockquote` или как отдельный абзац — как в источнике),
     в нём: имя автора — жирным курсивом, его описание/регалии — курсивом.
     НЕ бояться дублирования с блоком «Об авторе» (`<KLBLOCK …authors_about/>`):
     это два РАЗНЫХ описания (из источника — на странице лекции, стандартное
     — через KLBLOCK), дублирование НОРМАЛЬНО и даже ОЖИДАЕМО.
  2. **Стандартное описание автора** в базе «Элементов» — через
     `<KLBLOCK eltclub_authors_about/>`. Иногда обновляется (смена работы
     автора и т.п.) и может отличаться от описания в источнике — это
     НОРМАЛЬНО; текст описания лекции при этом НЕ меняем.

  Подзаголовок (лекторий/цикл и т.п.) из источника — НЕ теряем: включаем
  или в заголовок, или в текст описания (обычно лучше в текст, и хорошо —
  сразу после блока об авторе). Не выбрасываем его только потому, что он
  уже есть в шапке.
  В том числе оставляем информацию об авторе.

## 4. Описание автора

У каждого автора бывает два описания:

1. **Описание из источника** — как автора представляют организаторы прямо
   сейчас. Оно сохраняется в тексте описания лекции (обычно в аннотации).
   Если оно стоит отдельно (заголовок «ЛЕКТОР» + абзац) — стараемся немного
   интегрировать в текст («Лекцию прочитает …» и далее информация об авторе).
2. **Стандартное описание автора** в базе «Элементов» — выводится через
   `<KLBLOCK eltclub_authors_about/>`. Иногда обновляется (смена работы автора
   и т.п.) и может заметно отличаться от описания в источнике — это НОРМАЛЬНО.
   Описание в тексте лекции при этом НЕ меняем; разницу можно отметить
   комментарием под прототипом для нас.

Если автора нет в данных «Научного календаря» (нет в `data/events.json`,
в поле `speakers`) И НЕТ в базе «Элементов» — готовим краткую справку
(прототип описания автора для базы «Элементов»). КLBLOCK в описании стоит
ВСЕГДА, независимо от наличия автора в базе; если автора ещё нет — информация
о нём добавляется прежде, чем событие публикуется.

  - Имя полностью (по полу, падежам — как в исходнике);
  - Деятельность/должность/регалии — по материалам источника
    (строго из того, что есть; не выдумывать);
  - Если данных мало — так и написать «данных о биографии в источнике нет».

  HTML-блок автора на сайте выглядит так (повторяется на каждого):

  ```
  <div class='beige outblock_phone about_ins_block'><div class='about authors'>
    <div class='ib_img'><div class='img'><img src='…' alt='Фамилия Имя'></div></div>
    <div class='ib_text'>
    <div class='pretitle'>Фамилия Имя Отчество</div>
    <div class='text'><p>Биография…</p></div>
    </div>
    <div class='cl'></div>
  </div></div>
  ```

  При отсутствии фото — блок с фото можно опускать, оставляем текстовую часть.

  ## 5. Формат вывода (в чат)

  Выводим результат ТРЕМЯ блоками:

  1. **Формальные поля** — отдельный блок (раздел 2): без номеров, с HTML
     (`&nbsp;`, `<br>`), готовый к вставке в админку.
  2. **Текст описания** — читаемый вариант с лёгкой разметкой (курсив,
     жирный), без HTML-тегов.
  3. **HTML-код описания** — только внутренние блоки (без обвязки div):
     шапка, KLBLOCK, blockquote, — в блоке кода.

  ## 6. Что делать с результатом

  - Результат показывается в чате для проверки пользователем — НА САЙТ
    пока ничего не заносится автоматически.
  - После одобрения текста пользователем — обсудить и зафиксировать
    правила/примеры в этом скилле (раздел «История правил»).

  ## История правил

  - 20.09.2026 — закреплено правило про АДРЕС: адрес (улица, дом) НЕ
    вносим в текст описания и тем более в его шапку над блоком об авторе.
    Адрес — ТОЛЬКО в формальном поле «Место»: это поле и есть его место.
    Согласовано поле «Место» для «Прямой речи» на прототипе 21.09.2026.
    Низ описания (адрес, проезд, телефон и т.п.) пока НЕ формируем —
    как его составлять, обсудим отдельно (см. замечание 4 к прототипу).
  - 19.09.2026 — создан прототип описания лекции «Нейробиология боли»
    (lectory2.ru, лекция 967, Лекторий «Живой город», Тверь). Закреплено
    по итогам обсуждения:
    - класс шапки — `small` (допустимы `small` и `Small`);
    - HTML-фрагмент — без обвязки из div (только внутренние блоки);
    - формальные поля — отдельным блоком, без номеров, с HTML (`&nbsp;`,
      `<br>`), в строгом порядке 1–9;
    - абзацы источника не переставляем без важных причин;
    - «Поговорим о…» и схожие фразы НЕ добавляем, если их нет в источнике;
    - перечень тем — ненумерованный список `<ul><li>…</li></ul>`;
    - время окончания не ставим, если не указано ни прямо, ни косвенно;
    - явные опечатки источника исправляем и помечаем комментарием
      под прототипом страницы;
    - «расскажет» курсивом не выделяем (выделяем только описание автора
      и имя автора);
    - HTML-код всегда точно соответствует тексту (те же слова, абзацы,
      пунктуация; в HTML — сущности вроде `&nbsp;`);
    - неразбиваемые пробелы: после однобуквенных предлогов и союзов
      («в», «с», «к», «о», «у», «и», «а») ставим («и&nbsp;какой»,
      «а&nbsp;иногда»); после «но», «не» и двухбуквенных предлогов/союзов
      не ставим.
  - 19.09.2026 — зафиксировано: у каждого автора бывает два описания
    (одного автора): из источника (сохраняем в тексте лекции) и стандартное
    из базы «Элементов» (выводится через `<KLBLOCK eltclub_authors_about/>`);
    они могут отличаться — это нормально, текст не меняем; отдельный прототип
    описания автора для базы «Элементов» нужен только когда автора в базе
    ещё нет. `<KLBLOCK>` в описании стоит ВСЕГДА, независимо от наличия
    автора в базе. См. раздел 4.
  - 19.09.2026 — лишнее форматирование источника убираем: жирный
    заголовок-абзац, `<strong>` и т.п. не переносим в описание (наше
    форматирование должно быть стандартным). Исключение — выделенные
    в источнике пункты перечня можно оставить жирными (как в лекции
    Никитина).
  - 19.09.2026 — НЕ сокращаем и НЕ переписываем текст описания без
    надобности: переносим из источника ВСЁ существенное (все абзацы,
    все пункты списков, подзаголовок-лекторий, формулировки), ничего
    не теряем. Сокращение/переформулировка — только если есть важная
    причина (например, прямая инструкция проекта), и тогда — с
    комментарием под прототипом. Потерянные ранее абзацы (например,
    блок про «ВРЕМЯ у нас в запасе») — восстанавливаем из источника.
  - 19.09.2026 — подзаголовок (лекторий/цикл и т.п.) и абзац об авторе
    из источника НЕ теряем: подзаголовок включаем в текст (обычно сразу
    после блока об авторе или в шапку), абзац об авторе переносим в текст
    описания, не боясь дублирования с KLBLOCK; имя автора — жирным
    курсивом, его описание — курсивом.
  - 20.09.2026 — АДРЕС (улица, дом) в текст описания и его шапку НЕ
    вносим; его место — только формальное поле «Место». Низ описания
    (адрес, проезд, телефон и т.п.) пока не заполняем — как его составлять,
    обсудим отдельно.
  - 20.09.2026 — источник может держать данные лекции в JSON-LD (schema.org):
    bio автора, цены, продолжительность и т.п. даже если на странице они не
    выводятся. По правилу «ничего не теряем» такие данные вносим в текст
    описания полностью (например, абзац об авторе из поля bio JSON-LD
    лекции Сурдина «Марс: зона возможной жизни»). Цена в шапку текста НЕ
    ставится — она остаётся в формальных полях (см. п. 5).