---
name: event-description
description: "Составление по материалам источника (сайт организатора, Timepad) описания научно-популярного события, которого ещё нет на elementy.ru: текст с HTML-разметкой как на сайте + материал для формальных полей админки + (при необходимости) описание автора. Использовать по запросу «описание по источнику», «составить описание события», «новое событие», «сделать анонс события»."
---

# Описание события из источника (нет на «Элементах»)

Задача: по описанию события на сайте организатора или на Timepad собрать
полный «паспорт» события для «Научного календаря». События нет на
elementy.ru, поэтому работа идёт по исходным материалам источника.

В отличие от парсинга «Элементов», описание здесь создаётся С ИСПОЛЬЗОВАНИЕМ
ИИ (не алгоритмически): пользователь даёт текст/ссылки источника, мы по ним
пишем описание, следуя формату ниже. Правила и примеры накапливаются
в этом скилле по мере обучения.

## 0. Порядок работы

1. Пользователь присылает источник (ссылка или скопированный текст — сайт
   организатора, Timepad, страница ВК и т.п.). В дальнейшем нужно будет
   собирать новые события с самих источников.
2. Если есть ссылка — открыть её и взять данные.
3. Собрать «формальные поля» (раздел 2) — отдельным блоком, готовым
   к вставке в админку.
4. Написать текст описания события (раздел 3) с HTML-разметкой как на сайте.
5. Если автора события в данных «Научного календаря» ещё нет — подготовить
   описание автора (раздел 4).
6. Вывести результат в чат (раздел 5): формальные поля, текст с разметкой
   и HTML-код описания.
7. Спорные места (новое поле, неоднозначность) — спросить пользователя,
   потом зафиксировать решение в этом скилле.

## 1. Источники

- Сайт организатора (страница события) — основной источник; иногда может
  быть несколько страниц.
- Timepad — иногда единственный источник, иногда дополнительный, часто 
  именно здесь можно найти ссылку на регистрацию.
- Если данные в источниках расходятся — приоритет у сайта организатора;
  расхождение показать пользователю и спросить.

## 1а. УНИВЕРСАЛЬНЫЙ шаблон ответа (все лектории)

Шаблон ответа (формальные поля → текст → HTML, разделы 2–4) ОДИН для всех
лекториев, независимо от того, где беру событие. Нюансы бывают ТОЛЬКО в
разборе источника (разд. 1б), но описание события формируется одинаково. 
«Онлайн», nbsp, ёлочки, KLBLOCK — обрабатываются одинаково везде.

## 1б. Нюансы разбора источника — по лекториям

Различия между лекториями затрагивают ТОЛЬКО то, откуда берём данные и
какие поля есть (формальные поля и структура ответа — единые, см. 1а).

Карточки лекториев — в файле `reference/lectories.md` (по карточке на
лекторий: подзаголовок, ID-правило, онлайн-политика, место по умолчанию,
особенности разбора). При разборе события открыть карточку нужного
лектория и следовать ей. Новые правила лектория записываются сразу
в его карточку, а не дублируются здесь.

Сейчас есть карточки: «Живой город» (Тверь), «Прямая речь» (Москва),
Astrocamp (Москва), Фестиваль NAUKA 0+, «Центр "Архэ"». Другие лектории —
добавляем по мере появления; шаблон ответа при этом НЕ меняем.

## 2. Формальные поля (админка)

Выводятся ОТДЕЛЬНЫМ блоком, БЕЗ номеров и названий полей, но в строгом
порядке. Блок готов к вставке в админку: с `&nbsp;`, `<br>` и т.п.

Порядок полей:
 0. ID события
 1. Привязка (инвертированная дата + индекс времени)
 2. Время и место: дата, город, площадка [и ОНЛАЙН], время
 3. Полный заголовок с подзаголовком: подзаголовок(лекторий, цикл и т.п., тип 
    (лекция и т.п.), автор, название (в кавычках);
 4. Полный заголовок без подзаголовка;
 5. Только название лекции (без кавычек);
 6. Подзаголовок (лекторий/организатор/цикл лекций);
 7. Дата и время;
 8. Место;
 9. Источники (URL);
 10. URL регистрации/покупки билета;
 11. Стоимость;
 12. Возрастные ограничения;
 13. Адрес;
 14. Аудитория (ФАКУЛЬТАТИВНОЕ поле, выводится только когда по источнику
     можно определить аудиторию).

 Про поле 14 «Аудитория»: на «Элементах» в описании события есть поле
 «аудитория» с вариантами «для всех» / «для ученых» / «для студентов» /
 «для школьников». В источниках об этом почти никогда не пишут, но если
 вывод сделать можно — заполняем. Например, на страницах фестиваля NAUKA 0+
 строка «будет интересно всем» → «для всех». Если поле не заполнено —
 оно просто отсутствует в списке (не выводим «—»).

В формальных полях не бывает выделений жирным и курсивом, но бывают <br> и &nbsp;.

 В формальных полях используем КОРОТКИЕ наименования организаций
 (например, «Фундаментальная библиотека МГУ»), в первом абзаце шапки
 описания — ПОЛНЫЕ («Фундаментальная библиотека МГУ им.&nbsp;М.&nbsp;В.&nbsp;Ломоносова»).
 nbsp внутри названия организации НЕ ставим (обычные пробелы), кроме
 стандартных случаев (инициалы, числа, однобуквенные предлоги).

Формальные поля выводятся ПОСЛЕДОВАТЕЛЬНО, в двух видах (не колонками):
- для чтения: каждое поле в отдельном абзаце, с названием поля, внутри
  абзаца выводится содержимое (<br> заменяется переводом строки,
  &nbsp; пробелом);
- для копирования: в блоке кода, каждое поле на новой строке, БЕЗ названий
  полей — только значения, готовые к вставке в админку (с &nbsp;, <br>).


Пример (лекция «Нейробиология боли», lectory2.ru):

```
2026.09.196
19.09.2026, Тверь, Филармония, 18:00
Лекторий «Живой город»<br>Лекция Владимира Алипова «Нейробиология боли»
Лекция Владимира Алипова «Нейробиология боли»
Нейробиология боли
Лекторий «Живой город»
Суббота, 19 сентября 2026&nbsp;года, 18:00
Тверь, Филармония
https://lectory2.ru/#lecture_967
https://lectory2.ru/#lecture_967
1400&nbsp;руб.
16+
```

ВАЖНО:
Факт в формальное поле заносится ТОЛЬКО если он подтверждается источником.
Если поле неизвестно — писать «—» и не выдумывать.

## 3. Текст описания и HTML-разметка (как на сайте)

В выводимый HTML-фрагмент обвязка НЕ включается: ни
`<div class='itemblock memo'>`, ни сепаратор `<div class='cl'></div>` —
только внутренние блоки (шапка, KLBLOCK, blockquote, при необходимости
цена/ссылки).

Подробнее о формальных полях:

0. Идентификатор события, присваиваемый ему в начале работы в зависимости от источников,
   URL на них и id на источнике (если есть). Если источник — Timepad: TP-<id Timepad>
   (например, TP-4161306);
1. **Привязка:** поле начинается с инвертированной даты через точки, например, для 19 
   сентября 2026: 2026.09.19;
   в конце приписываются 1 или 2 цифры. Первая соответствует часу: 12 или раньше -0, 
   13 - 1, 14 - 2, ... 21 или позже - 9. Вторая ставится только если время начала 
   содержит минуты: 00 - не ставится, 30 - 6, от 01 до 29 - 4, больше 30 - 8. 
   Например, в указанную дату 19:00 - 2026.09.197, 19:30 - 2026.09.1976, 
   19:45 - 2026.09.1978
2. **Время и место:** через запятую дата (дд.мм.гггг), город, площадка (если есть
   онлайн-трансляция, добавляется без запятой "и ОНЛАЙН"), время начала события
   (без времени окончания). Если мероприятие проводится ТОЛЬКО онлайн —
   после времени начала дописать часовой пояс: «29.09.2026, ОНЛАЙН, 18:30 (мск)».
   Часовой пояс указываем В ЭТОМ поле и только в нём (в других полях не дублировать).
3. **Полный заголовок с подзаголовком:** подзаголовок -- название лектория, цикла
   лекций и т.п. (если есть); после подзаголовка <br>; тип мероприятия
   (лекция, фестиваль, экскурсия и т.п.); если лекция, автор в родительном падеже 
   (лекция Сергея Попова), если другое мероприятие, может быть другой формат
   (например, встреча с Сергеем Поповым); название в кавычках.
   ВАЖНО: формат этого поля не такой, как у похожей на него второй строки шапки описания. 
   Здесь подзаголовок всегда в начале, в именительном падеже и отделяется <br> 
   (а там в конце и синтаксически связывается с названием)
4. **Полный заголовок без подзаголовка:** то же, но без подзаголовка. 
   Если подзаголовка нет, совпадает с предыдущим полем.
5. Только название лекции (без кавычек);
6. **Подзаголовок** (лекторий/организатор/цикл лекций); тот же, что в поле 3.,
   может отсутствовать, тогда прочерк.
7. **Дата и время** (со временем окончания, если известно);
8. **Место**: город, площадка. Если есть онлайн-трансляция, добавляется "и ОНЛАЙН".
   Если ТОЛЬКО онлайн-трансляция, в этом поле просто ОНЛАЙН;
   это поле не включает адрес (улицу, дом и т.п.)
9. **Источники** (URL): URL источников, полученные от пользователя или найденные,
   записываются во временный файл сразу при начале обработки события, чтобы потом
   поместить их в это поле. Если источников несколько, каждый на новой строке.
10. **URL регистрации/покупки билета**: если среди источников есть Timepad,
   к его URL добавляется #register
   если на источнике (кроме Timepad) есть кнопка регистрации/покупки билета с 
   переходом на другую страницу, URL этой страницы
   если есть более одной кнопки, ведущих на другие страницы, URL всех этих страниц
   (каждый на новой строке)
   если анкета регистрации прямо на странице или кнопка открывает модальное
   окно, URL регистрации совпадает с URL источника
11. **Стоимость:** если вход свободный и т.п., бесплатно;
   если указан диапазон (от 2000 до 2500 рублей), указываем нижнюю
   границу (от 2000 руб.);
   если указана полная цена и цена со скидкой (например, ранняя), рассматриваем 
   это как диапазон цен, пишем от меньшей цены;
   если есть трансляция, указываем стоимость посещения мероприятия и дополнительно
   трансляции (1300 руб., онлайн 500 руб.);
   если указаны льготные цены, добавляем их:
   (1300 руб., студентам бесплатно, онлайн 500 руб.);
   для рублей всегда используем сокращение руб., после числа неразбиваемый пробел.
12. **Возрастные ограничения**: ставим только если указаны;
 13. **Адрес**: город, улица, дом, этаж/зал/аудитория (что есть в источнике).
 14. **Аудитория** (факультативно): только если по источнику она ясна;
    варианты значений — «для всех» / «для ученых» / «для студентов» /
    «для школьников». Если поле не заполняем — не выводим.


Структура описания:

1. **Шапка, абзац 1** — формальная строка:
   `<p class="small"><b>Суббота, 19&nbsp;сентября 2026&nbsp;года, 18:00, Тверь.</b></p>`
   - день недели (с большой буквы), дата «ДД месяц ГГГГ года»;
   - время начала; время окончания ставим только если известно явно
      или косвенно (указана продолжительность);
   - место: город (и площадка, если известна — указываем ВСЕГДА, когда
      она есть: «Москва, Фундаментальная&nbsp;библиотека&nbsp;МГУ
      им.&nbsp;М.&nbsp;В.&nbsp;Ломоносова») или ОНЛАЙН; если есть и
      очное мероприятие, и онлайн, добавляем после места: и ОНЛАЙН.
   - в конце точка.
   Класс — `small` (нижний регистр; вариант `Small` тоже допустим).
2. **Шапка, абзац 2** — тип мероприятия обычным шрифтом, автор
   в родительном падеже без отчества жирным курсивом, название в «ёлочках» жирным,
   цикл, лекторий и т.п.,если есть (связывается синтаксически с названием),
   в конце точка:
   `<p>Лекция <b><i>Владимира Алипова</i> «Нейробиология боли»</b> в лектории «Живое слово».</p>`
3. **Блок об авторе** — заглушка, которую подставляет система:
   `<KLBLOCK eltclub_authors_about/>`
   НЕ НУЖНО вставлять на этом месте код реального блока, только эта заглушка!
4. **Аннотация**: заключена в тег `<blockquote class="small">…</blockquote>` 
   Заполняется по материалам источника:
   - абзацы источника НЕ переставляем без необходимости: описание меняем только 
     если для этого есть важные причины;
   - Все упоминания автора: описание автора — курсивом, имя автора (в формате 
     Имя Фамилия) — жирным курсивом:
      `<p><i>Нейробиолог <b>Владимир Алипов</b></i> расскажет:</p>`
      Лишнее форматирование источника убираем (жирный заголовок абзаца,
      `<strong>` и т.п.) — наше форматирование должно оставаться
      стандартным; исключение — выделенные в источнике пункты перечня
      (как в лекции Никитина), их можно оставить;
   - автор в аннотации всегда без отчества, по возможности имя, а не инициалы;
   - перечень тем, о которых расскажут, и другие списки — ненумерованным списком
     `<ul><li>…</li>…</ul>`;
   - явные опечатки источника исправляем и помечаем комментарием
     под прототипом страницы.
5. **Конец HTML-кода описания** — блок описания заканчивается на
   закрывающем теге аннотации `</blockquote>`. Всё, что идёт после
   аннотации (цена и ссылки на билеты/регистрацию, трансляция, адрес,
   способы связи, ссылки на источники), в блок описания НЕ входит:
   для него есть отдельный раздел «Дополнительная информация» (разд. 5а).
7. **Автор:** готовится для отдельного описания автора в базе "Элементов",
   только если автор новый (не найден на "Элементах"), 

   Формат описания автора:
   7.1. **Инвертированное имя:** Фамилия Имя; без тегов абзаца.
   7.2. **Стандартное имя:** Имя Фамилия; без тегов абзаца.
   7.3. **Полное имя:** Имя Отчество Фамилия; если отчество неизвестно, не заполняется;
       без тегов абзаца.
7.4. **Описание автора:** с заглавной (большой) буквы в именительном падеже, 
       без шрифтовых выделений, всё и только то, что об авторе сказано на странице
       события (может выводиться в модальном окне). Деятельность/должность/регалии —
       по материалам источника (строго из того, что есть; не выдумывать), сокращения
       вроде "к.ф.-м.н." раскрыть.
       Правила:
       - Должность согласуется с ПОЛОМ автора: если автор женщина,
         «заведующий кафедрой» → «заведующая кафедрой» (исправляем род,
         даже если в источнике должность дана в мужском роде).
       - Если автора называют по должности (заведующий/заведующая кафедрой,
         директор и т.п.), в описании ОБЯЗАТЕЛЬНО указывается и организация,
         где он/она занимает эту должность (если она известна из источника
         или его контекста — например, сам лекторий/организатор события).
         НЕ оставляем должность без организации.
7.5. **Фото из источников** в исходном виде (ссылки на картинки на страницах
         источников); если на источниках нет фото автора, поле не заполняется.
     7.6. **Файл фотографии:** имя файла фото автора для загрузки в базу.
        Приводится ГОТОВЫМ (чтобы можно было скопировать, а не вводить руками).
        Формат: `familiya-imya_120.jpg` — фамилия и имя латиницей в нижнем
        регистре, через дефис, суффикс `_120.jpg`.
     Поля 7.1, 7.2, 7.3 (если есть), 7.4 можно вывести только в блоке HTML-кода 
     (7.1, 7.2., 7.3 без тегов абзаца, без <br>).
     Поле 7.4 дополнительно выводится БЛОКОМ HTML-кода: то же описание
     внутри `<p>…</p>`, с `&nbsp;` и т.п. (по нему тоже прогонять post-check).

   Если авторов несколько, этот набор полей выводится для каждого.     

   NB: У каждого автора бывает два описания:
   1. **Описание из источника** — как автора представляют организаторы прямо
    сейчас. Оно сохраняется в тексте описания лекции (обычно в аннотации).
    Если оно стоит отдельно (заголовок «ЛЕКТОР» + абзац) — стараемся немного
    интегрировать в текст («Лекцию прочитает …» и далее информация об авторе).
   2. **Стандартное описание автора** в базе «Элементов» — выводится через
    `<KLBLOCK eltclub_authors_about/>`. Иногда обновляется (смена работы автора
    и т.п.) и может заметно отличаться от описания в источнике — это НОРМАЛЬНО.
    Описание в тексте лекции при этом НЕ меняем; разницу можно отметить
    комментарием под прототипом для нас.

При написании ВСЕХ ПОЛЕЙ текста применяются корректорские правила из скилла
`proofreading` (ё, неразбиваемые пробелы, кавычки, тире).

ВАЖНО: текст описания НЕ сокращаем и НЕ переписываем без надобности.
Переносим из источника(ов) ВСЁ существенное: все абзацы, все пункты
списков, подзаголовки, формулировки. Ничего из источника не теряем.
Допустимо только то, что прямо предписывают правила проекта
(например, включить подзаголовок в начало текста) — и тоже без потери
смысла.

В описании используются только абзацы <p> и списки <ul><li>. Тег <blockquote>
используется только для оформления аннотации в целом. Если в источнике 
используются другие теги (<blockquote>, <div>), они преобразуются в 
обычные абзацы.

Технические поля (как попасть на мероприятие, необходимость регистрации,
трансляция и т.п.), адрес с проездом, способы связи и ссылки на источники
выносим в раздел «Дополнительная информация» (см. разд. 5а).

Подзаголовок (лекторий/цикл и т.п.) из источника — НЕ теряем: включаем
или в заголовок, или в текст описания (обычно лучше в текст, и хорошо —
сразу после блока об авторе). Не выбрасываем его только потому, что он
уже есть в шапке.

## 4. Проверка, есть ли автор:

Считаем, что автор есть, если мы добавляли с "Элементов" событие с ним
или готовили описание с таким событием.

## 3а. Лекции курсов (цикл лекций)

Описание лекции из КУРСА (цикла лекций) отличается от обычной лекции:

1. Во второй строке шапки, перед блоком «об авторе» (KLBLOCK), указываем
   название КУРСА, а не лекции (образец — «Элементы», 449663, первая
   лекция курса Квартальнова):
   `<p>Культурно-просветительский центр «Архэ» приглашает на курс
   <b><i>Павла Квартальнова</i> «Зимняя экология: снег, холод и&nbsp;тепло»</b>.</p>`
2. После блока «об авторе», перед аннотацией — ОБЯЗАТЕЛЬНЫЙ абзац
   с названием лекции: «продолжим говорить о...» (для непервой лекции)
   или «Тема первой лекции: <b>«Название лекции»</b>». Перед ним сразу
   KLBLOCK; далее аннотация в «ёлочках».
3. В конце аннотации, перед закрывающим `</blockquote>`, добавляется
   ПЛАН КУРСА — нумерованный список `<ol><li>…</li>…</ol>` с номерами
   лекций и их темами (и датами, если известны).
4. ТОЛЬКО у первой лекции курса указывается стоимость курса в целом
   (и, если есть, цена подписки на трансляции всего курса); у остальных
   лекций курса это не указывается.
5. ФОРМАЛЬНЫЕ ПОЛЯ 3–6 у лекций курса (схема записана пользователем
   26.09.2026 при прототипе Усанова, TP-4206645):
   - поле 3 (полный заголовок с подзаголовком):
     «Название лекции»<br>Лекция N курса Автора «Название курса»
   - поле 4 (полный заголовок без подзаголовка):
     «Название лекции». Лекция N курса Автора «Название курса»
   - поле 5 (только название): Название лекции (без кавычек)
   - поле 6 (подзаголовок): Название курса. Лекция N
   Номер — цифрой, неразбиваемый пробел ПЕРЕД ним: «…Лекция&nbsp;1».
   Число относится к одному соседнему слову — к «Лекция» (nbsp ставлю
   только перед числом), поэтому после числа — обычный пробел:
   «Лекция&nbsp;1 курса…», НЕ «Лекция&nbsp;1&nbsp;курса».
   Пример для курса «онлайн-курс Павла Усанова» (первая лекция
   «Великобритания. Как маленький остров изобрел Промышленную
   революцию»):
   - поле 3: «Великобритания. Как маленький остров изобрел Промышленную
     революцию»<br>Лекция&nbsp;1 курса Павла Усанова «„Пять историй
     богатства“: как страны становятся великими и&nbsp;почему теряют свой
     успех»
   - поле 4: «Великобритания. Как маленький остров изобрел Промышленную
     революцию». Лекция&nbsp;1 курса Павла Усанова «„Пять историй
     богатства“: как страны становятся великими и&nbsp;почему теряют свой
     успех»
   - поле 5: Великобритания. Как маленький остров изобрел Промышленную
     революцию
   - поле 6: «Пять историй богатства»: как страны становятся великими
     и&nbsp;почему теряют свой успех. Лекция&nbsp;1
   Кавычки вокруг «Название курса»: у этого курса в названии УЖЕ есть
   «ёлочки» («Пять историй богатства»), поэтому вокруг всего названия
   курса ставим только «ёлочки», а внутренние превращаем в „лапки“
   (см. `proofreading`, п. 3 «Кавычки»). Результат:
   «„Пять историй богатства“: как страны становятся великими
   и&nbsp;почему теряют свой успех». НЕ писать ««Пять историй богатства»»
   (два знака « подряд).
   Поле 6 в образцы «Элементов»: «Зимняя экология: снег, холод
   и&nbsp;тепло. Лекция&nbsp;1», «На острие науки. Астрономия
   и&nbsp;современная физика. Лекция&nbsp;5» (449663, 450094).

Если нет, запускаем поиск на "Элементах", используя URL:
`https://elementy.ru/find?objs=eltevents&words=Имя+Фамилия`
(подставляем имя и фамилию автора, например, `Алексей+Паевский`).
В списке найденного должны быть события, для которых данный автор упомянут
в последней строке, например: 
`Научный календарь  • Анна Хоружая, Алексей Паевский • 19.09`


## 5. Формат вывода (в чат)

ПРАВИЛО СООТВЕТСТВИЯ: сначала готовим HTML-код, потом в тексте для показа
выводим его с точностью до согласованных изменений (у абзацев, которые 
предназначены для помещения в однострочные поля, убираем теги абзаца).

HTML-блоки (блоки 2, 4, 6 и блок «Об авторе») выводим ВСЕГДА ПОЛНОСТЬЮ:
без сокращений «…», без пропусков абзацев и пунктов. Если блок уже был
показан и требуется перевывести его (исправление, переспрос) — повторяем
полный блок целиком, а не его фрагмент.

Выводим результат несколькими блоками:

1. **Формальные поля: для чтения** — отдельный блок (раздел 2): каждое поле
  с названием в новом абзаце, внутри абзаца выводится содержимое поля
  (<br> заменяется переводом строки, &nbsp; пробелом)
2. **Формальные поля: для копирования** — в блоке кода, каждое поле на новой
  строке, БЕЗ названий полей, с <br>, &nbsp;
3. **Текст описания** — читаемый вариант с лёгкой разметкой (курсив,
  жирный), без HTML-тегов. Заканчивается на конце аннотации.
4. **HTML-код описания** — только внутренние блоки (без обвязки div):
  шапка, KLBLOCK, blockquote (до `</blockquote>` включительно), — в блоке кода.
5. **Дополнительная информация: текст** — читаемый вариант без тегов:
  трансляция, регистрация/покупка билетов, адрес (и проезд), способы
  связи, ссылки на источники и полезные ссылки (что есть у события).
6. **Дополнительная информация: HTML-код** — те же блоки абзацами `<p>`
  со ссылками и &nbsp;, в блоке кода (как в п. 4, без обвязки div).
7. **Об авторе** (если автора нет).

В блоке 4. (HTML-код описания) вставляем пустые абзацы между всеми абзацами 
 <p> и пунктами списка <li>, тегами блока об авторе <KLBLOCK>, а также сразу
 после открывающего и перед закрывающим тегами <blockquote>. ВНУТРИ <ul>
 пустые абзацы не вставляем: после <ul> и перед </ul> пустая строка НЕ нужна
 (раньше ошибочно ставились). Тот же принцип — в блоке 6 (между <p>
 пустая строка).

 Абзац «Лекцию прочитает …» (указание автора с его ПОЛНЫМ описанием)
 ставим В КОНЕЦ аннотации: последним абзацем внутри <blockquote>, сразу
 перед закрывающим тегом (а не первым абзацем аннотации и не за пределами
 <blockquote>). Сначала смотрим, ЕСТЬ ли в источнике описание автора:
 - описания автора в источнике НЕТ (ни отдельным блоком, ни в тексте) —
   НИЧЕГО не добавляем: текст лекции оставляем как есть, описание автора
   не выдумываем;
 - описание автора ЕСТЬ — отдельным блоком (как «Лекторы/ведущие»
   на фестивале NAUKA 0+) или в самом тексте лекции (например, в конце,
   дублируя упоминания в начале/середине) — в обоих случаях действуем
   как с отдельным описанием, по представленности лектора в тексте:
   • лектор в тексте НЕ упоминался → добавляем закрывающий абзац
     «Лекцию прочитает …» с полным описанием;
   • лектор упомянут с ПОЛНЫМ/достаточным описанием — например,
     «с&nbsp;<b><i>Сергеем Марковым</i></b>&nbsp;— директором по&nbsp;развитию
     технологий ИИ Сбера» — закрывающий абзац НЕ нужен (замечание
     пользователя 25.09.2026 по лекции Маркова «Съедят ли электроовцы
     людей?…»);
   • лектор упомянут БЕЗ описания или с существенно неполным описанием —
     ОБЯЗАТЕЛЬНО восполнить: либо дополнить описание в том месте, где он
     упоминался (предпочтительно — меньше дублирования), либо добавить
     закрывающий абзац «Лекцию прочитает …» с полным описанием.
     Полное описание автора берём там, где оно есть в источнике
     (блок «Лекторы/ведущие», конец текста лекции и т.п.).

## 5а. Дополнительная информация (после аннотации)

Прототип делится на три части: формальные поля (разд. 2), описание
события (шапка + KLBLOCK + аннотация; HTML-код заканчивается на
`</blockquote>`) и «Дополнительная информация» — всё, что идёт после
аннотации.

В «Дополнительную информацию» выносим (что есть у события; собираем
из источника, ничего не теряем):

- **Трансляция** — «Будет организована интернет-трансляция лекции…»,
  ссылка на страницу трансляции;
- **Регистрация / покупка билетов** — по образцу «Элементов»:
  `<p>Стоимость билета: <b>1300&nbsp;руб.</b> <a href="..."
  target="_blank">Купить билет</a>.</p>`
  Стоимость — как в формальном поле «Стоимость» (нижняя граница,
  отдельно трансляция и т.п.); сюда же уместно 16+, скидки, условия
  просмотра записи и прочие примечания источника;
- **Адрес** (город, улица, дом, зал/аудитория) и, если есть в источнике,
  **проезд** (метро и т.п.); у только-онлайн событий адреса нет;
- **Способы связи** — телефон, почта, Telegram и т.п.;
- **Ссылки на источники и полезные ссылки** — как на «Элементах»:
  «Информация о лекции на Timepad», «Лекторий …», «Карта» и т.п.

Формат вывода — как у формальных полей и описания, последовательно двумя
видами (не колонками): текстовый вид (читаемо, без тегов) и HTML-код
(абзацы `<p>` со ссылками `<a href="…" target="_blank">…</a>` и &nbsp;,
в fenced-блоке, между абзацами — пустая строка, как в блоке 4).

Текст «Дополнительной информации» проходит те же правила `proofreading`
и `post-check`, что и описание.

## 6. Что делать с результатом

- Результат показывается в чате для проверки пользователем — НА САЙТ
  пока ничего не заносится автоматически.
- После одобрения текста пользователем — обсудить и зафиксировать
  правила/примеры в этом скилле (раздел «История правил»).

## История правил

- 26.09.2026 — часовой пояс (мск): если мероприятие проводится ТОЛЬКО
  онлайн, в поле 2 «Время и место» после времени начала дописываем «(мск)»:
  «29.09.2026, ОНЛАЙН, 18:30 (мск)». Только в этом поле, в других не
  дублировать. Записано пользователем при разборе прототипа Усанова
  (TP-4206645); прототип не переделывается (на будущее).
- 26.09.2026 — уточнена схема полей 3–6 лекций КУРСА (см. разд. 3а п. 5):
  поле 3 = «Название лекции»<br>Лекция N курса Автора «Название курса»;
  поле 4 = «Название лекции». Лекция N курса Автора «Название курса»;
  поле 5 = Название лекции (без кавычек); поле 6 = Название курса.
  Лекция N. Записана пользователем при прототипе Усанова (TP-4206645).
  Тогда же выяснено про кавычки: если название курса само содержит
  «ёлочки», вокруг всего названия ставим «ёлочки», внутренние — „лапки“
  (НЕ ««Пять историй богатства»», а «„Пять историй богатства“»).
- 26.09.2026 — еще одно правило лекций КУРСОВ (дополнение к разд. 3а):
  подзаголовок (поля 3 и 6) у лекций курса — «Название курса. Лекция N»,
  а НЕ лекторий (образцы «Элементов» 449663, 450094). Записал пользователь
  при прототипе Усанова (первая лекция курса «Пять историй богатства»,
  TP-4206645). Пост-проверка это правило не ловит: она проверяет механику
  (nbsp/«ё»/тире/кавычки), а не содержание полей.
- 26.09.2026 — правило nbsp для ЧИСЛА между двумя словами (в т.ч. в полях
  3/4 лекций курса): число относится к одному соседнему слову — либо
  слову ПОСЛЕ («1&nbsp;миллион»), либо слову ДО («Лекция&nbsp;1 курса…»).
  Со стороны слова, к которому относится число, ставим nbsp, с другой
  стороны — обычный пробел. «Лекция&nbsp;1&nbsp;курса» — ошибка (число
  привязано к обоим словам). Исключение: однобуквенный предлог слева от
  числа («В&nbsp;2025&nbsp;году» — левый nbsp обязателен после «в» и число
  относится к слову справа). Записано пользователем при прототипе Усанова
  (TP-4206645); зеркало в `tools/check_prototype.py`
  (`p_nbsp_double_number`, тег 1d; уточнение 1a).
- 25.09.2026 — правила лекций КУРСОВ пользователем (зафиксированы в разд.
  3а): в шапке — название курса (не лекции); после KLBLOCK — обязательный
  абзац с названием лекции; в конце аннотации перед `</blockquote>` —
  план курса (нумерованный `<ol>`); цена на курс в целом (и подписку
  на трансляции всего курса) указываем ТОЛЬКО у первой лекции.
- 25.09.2026 — формальные поля выводятся ПОСЛЕДОВАТЕЛЬНО двумя видами
  (не колонками): сначала «для чтения» (каждое поле с названием в отдельном
  абзаце), потом «для копирования» (в блоке кода, БЕЗ названий полей).
  В «Дополнительной информации» тот же принцип: текст, затем HTML-код.
  (Формулировка уточнена пользователем 25.09.2026 — «не колонками, а
  сначала для чтения, потом для копирования».)
- 25.09.2026 — HTML-блоки прототипа выводить ВСЕГДА ПОЛНОСТЬЮ: без «…»,
  без пропусков абзацев и пунктов; при перевыводе — повторять блок целиком
  (записал пользователь при прототипе Масленникова, TP-4207848).
- 25.09.2026 — прототип делится на три части: формальные поля, описание
  (шапка + KLBLOCK + аннотация) и «Дополнительная информация». HTML-код
  описания заканчивается на `</blockquote>`; всё, что идёт после аннотации
  (трансляция, регистрация/покупка билетов, адрес и проезд, способы
  связи, ссылки на источники и полезные ссылки), выносится в отдельный
раздел «Дополнительная информация» (разд. 5а) с двумя вариантами
   вывода — текст и HTML-код. Записано по просьбе пользователя при
   прототипе Масленникова (TP-4207848).
- 26.09.2026 — снова нарушено при выводе прототипа TP-4220408 (2-я лекция
  Усанова): «Дополнительная информация» (трансляция, стоимость, 16+,
  видеозаписи, контакты, ссылки на источники) оказалась включённой в блок 4
  (HTML-код описания). НАПОМИНАНИЕ: блок 4 строго заканчивается на
  `</blockquote>`; всё, что после аннотации, выводится двумя отдельными
  блоками 5 и 6 (текст и HTML-код). Никакие части «Дополнительной
  информации» в блок 4 не включаются, в т.ч. «16+» и «Информация о лекции…».
- 25.09.2026 — добавлена карточка лектория «Центр "Архэ"»
  (`reference/lectories.md`), правила источника от пользователя: основной
  источник — сайт организатора arhe.msk.ru, второй — Timepad (ДВА аккаунта:
  tsentr-arhe-v-sankt-peter и arhe-events); у лекций два формата страниц
  на arhe.msk.ru — `?p=<число>` (предпочтителен) и
  `?tribe_events=<транслит>`; ID — TP-<id Timepad>; обязательная проверка
  других вариантов лекции по календарю/разделам сайта и обоим timepad;
  в «Источниках» — все найденные варианты; онлайн/офлайн может различаться
  между аккаунтами, сверяем оба.
- 25.09.2026 — в блоке «Об авторе» добавлено поле 7.6 «Файл фотографии»:
  имя файла фото автора готовым для копирования, формат
  `familiya-imya_120.jpg` (латиница, нижний регистр, дефис, суффикс
  `_120.jpg`). Пример для Елены Лапшевой: `lapsheva-elena_120.jpg`.
- 25.09.2026 — замечания пользователя по лекции Сергея Маркова
  «Съедят ли электроовцы людей?…» и правило про абзац «Лекцию прочитает …»
  (полный текст — в разд. 5). Ключевые пункты:
  1) абзац «Лекцию прочитает …» — только когда лектор НЕ упоминался
  в тексте аннотации; если автор уже представлен с полным описанием
  («с&nbsp;<b><i>Сергеем Марковым</i></b>&nbsp;— директором по&nbsp;развитию
  технологий ИИ Сбера»), дублирующий закрывающий абзац НЕ нужен —
  из прототипа убран как избыточный;
  2) если лектор упомянут БЕЗ описания или с существенно неполным
  описанием — НЕ оставлять как есть: либо дополнить описание в том
  месте, где он упоминался, либо добавить закрывающий абзац «Лекцию
  прочитает …» с полным описанием;
  3) если описания автора в источнике НЕТ (ни отдельным блоком, ни
  в тексте) — НИЧЕГО не добавляем, описание автора не выдумываем;
  4) описание автора в КОНЦЕ текста лекции, дублирующее упоминания
  в начале/середине, — учитываем как отдельное описание: автор уже
  описан, закрывающий абзац не нужен.
- 24.09.2026 — в формальных полях — КОРОТКИЕ наименования организаций
  («Фундаментальная библиотека МГУ»), в первом абзаце шапки — ПОЛНЫЕ
  («Фундаментальная библиотека МГУ им.&nbsp;М.&nbsp;В.&nbsp;Ломоносова»).
- 24.09.2026 — в первом абзаце шапки описания указываем площадку ВСЕГДА,
  когда она известна (город, площадка): «Суббота, 10&nbsp;октября
  2026&nbsp;года, 17:15–18:15, Москва, Фундаментальная библиотека&nbsp;МГУ
  им.&nbsp;М.&nbsp;В.&nbsp;Ломоносова.».
- 24.09.2026 — абзац «Лекцию прочитает …» (автор с описанием) ставим
  В КОНЕЦ аннотации: последним абзацем внутри <blockquote>, перед
  закрывающим тегом, но ТОЛЬКО когда лектор раньше в тексте не
  упоминался (см. уточнение 25.09.2026).
- 24.09.2026 — исследования формата: мягкий перенос строк в fenced-блоке
  кода в десктоп-GUI opencode НЕВОЗМОЖЕН изнутри чата (CSS зашит в
  приложение: `[data-component="markdown"] pre` = `white-space:pre`,
  темы — только цвета, пользовательской инъекции CSS нет). Поэтому
  вывод кода — только fenced-блоками; чтобы длинные строки не уходили
  за край, строки HTML делаем короткими (пустые абзацы и т.п.).
- 24.09.2026 — уточнение: пустые абзацы в HTML-коде описания (блок 4)
  НЕ ставятся внутри <ul>: после <ul> и перед </ul> пустую строку НЕ
  вставляем (между <li> — можно); <blockquote> — вставка остаётся.
- 24.09.2026 — технические абзацы источника со ссылками на другие лекции
  цикла («Первая лекция данного цикла состоится… регистрация здесь») обычно
  в описание НЕ ставим. Исключение: прототип Astrocamp (вторая лекция,
  TP-4188800) — оставлена по согласованию с пользователем.
- 24.09.2026 — блокам «Формальные поля: HTML-код» и «HTML-код описания»
  сохраняем вывод В БЛОКЕ КОДА (fenced ```) — это гарантирует копирование
  кода без искажений для вставки в админку; вариант «обычным текстом»
  отвергнут пользователем (обрывает/смешивает строки при переносе).
- 24.09.2026 — индивидуальные правила лекториев вынесены в файл-карточки
  `reference/lectories.md` (по карточке на лекторий: подзаголовок,
  ID-правило, онлайн-политика, место по умолчанию, особенности разбора).
  В разд. 1б — только ссылка на файл; новые правила лектория пишем сразу
  в его карточку. Сейчас есть карточки: «Живой город», «Прямая речь»,
  Astrocamp.
- 24.09.2026 — разобран прототип Astrocamp (Timepad: лекция «Земное начало:
  как зарождалась жизнь», Юрий Сарычев). Решения:
  - ID для событий с Timepad: TP-<id Timepad>;
  - подзаголовок — «Лекторий Astrocamp»;
  - модалка «Получить ссылку на трансляцию» (есть в HTML, не видна на
    странице) подтверждением онлайн-участия НЕ считается — офлайн полностью
    по умолчанию для Astrocamp;
  - лекция проходит офлайн; аннотация строится по абзацам источника;
  - событие — библиотечное: программа, «В программе:» — жирное у источника
    снимается, список — `<ul><li>`; автор (любитель и популяризатор
    астрономии) — новый, на «Элементах» не найден.
- 24.09.2026 — уточнено: абзац-подзаголовок цикла из источника («Космос
  и&nbsp;жизнь: …») НЕ теряем — включаем в текст описания (первым абзацем
  аннотации), дублирование с шапкой допустимо/обсуждаемо.
- 24.09.2026 — замечания пользователя по прототипу Astrocamp (зафиксированы):
  - Формальные поля «для чтения» выводить ПО ОДНОМУ полю на абзац (0–13),
    а не сплошным текстом (если поля идут через простые переводы строк —
    они сливаются в один абзац).
  - В HTML-коде описания (блок 4) ОБЯЗАТЕЛЬНО пустые абзацы (пустые
    строки) между всеми <p> и <li>, вокруг <KLBLOCK>, а также сразу после
    открывающего и перед закрывающим тегами <blockquote>. Внутри <ul>
    пустых строк НЕ ставим (см. уточнение про <ul>).
  - Поле 7.4 (описание автора) дополнительно ВЫВОДИТЬ БЛОКОМ HTML-кода:
    то же описание внутри <p>…</p>, с &nbsp; и т.п.; по нему тоже
    прогонять post-check.
  - Если лектор раньше в тексте не упоминался, вместо «Лектор — <описание
    автора>» писать «Лекцию прочитает <описание автора>» (имя автора —
    жирным курсивом, описание — курсивом).
  - Типичные ошибки nbsp (проверять при post-check): nbsp ставятся ТОЛЬКО
    после однобуквенных предлогов/союзов («в», «с», «к», «о», «у», «и»,
    «а»); НЕ после «от», «до», «на», «во», «по», «из», «для» и других
    двух-трёхбуквенных; «Лекция&nbsp;1» (число — с nbsp), «с&nbsp;кафе».

- 20.09.2026 — закреплено правило про АДРЕС: адрес (улица, дом) НЕ
  вносим в текст описания и тем более в его шапку над блоком об авторе.
  Адрес — ТОЛЬКО в формальном поле «Место»: это поле и есть его место.
  Согласовано поле «Место» для «Прямой речи» на прототипе 21.09.2026.
  Низ описания (адрес, проезд, телефон и т.п.) пока НЕ формируем —
  как его составлять, обсудим отдельно (см. замечание 4 к прототипу).
- 19.09.2026 — создан прототип описания лекции «Нейробиология боли»
  (lectory2.ru, лекция 967, Лекторий «Живой город», Тверь). Закреплено
  по итогам обсуждения:
  - класс шапки — `small` (допустимы `small` и `Small`);
  - HTML-фрагмент — без обвязки из div (только внутренние блоки);
  - формальные поля — отдельным блоком, без номеров, с HTML (`&nbsp;`,
    `<br>`), в строгом порядке 1–9;
  - абзацы источника не переставляем без важных причин;
  - «Поговорим о…» и схожие фразы НЕ добавляем, если их нет в источнике;
  - перечень тем — ненумерованный список `<ul><li>…</li></ul>`;
  - время окончания не ставим, если не указано ни прямо, ни косвенно;
  - явные опечатки источника исправляем и помечаем комментарием
    под прототипом страницы;
  - «расскажет» курсивом не выделяем (выделяем только описание автора
    и имя автора);
  - HTML-код всегда точно соответствует тексту (те же слова, абзацы,
    пунктуация; в HTML — сущности вроде `&nbsp;`);
  - неразбиваемые пробелы: после однобуквенных предлогов и союзов
    («в», «с», «к», «о», «у», «и», «а») ставим («и&nbsp;какой»,
    «а&nbsp;иногда»); после «но», «не» и двухбуквенных предлогов/союзов
    не ставим.
- 19.09.2026 — зафиксировано: у каждого автора бывает два описания
  (одного автора): из источника (сохраняем в тексте лекции) и стандартное
  из базы «Элементов» (выводится через `<KLBLOCK eltclub_authors_about/>`);
  они могут отличаться — это нормально, текст не меняем; отдельный прототип
  описания автора для базы «Элементов» нужен только когда автора в базе
  ещё нет. `<KLBLOCK>` в описании стоит ВСЕГДА, независимо от наличия
  автора в базе. См. раздел 4.
- 19.09.2026 — лишнее форматирование источника убираем: жирный
  заголовок-абзац, `<strong>` и т.п. не переносим в описание (наше
  форматирование должно быть стандартным). Исключение — выделенные
  в источнике пункты перечня можно оставить жирными (как в лекции
  Никитина).
- 19.09.2026 — НЕ сокращаем и НЕ переписываем текст описания без
  надобности: переносим из источника ВСЁ существенное (все абзацы,
  все пункты списков, подзаголовок-лекторий, формулировки), ничего
  не теряем. Сокращение/переформулировка — только если есть важная
  причина (например, прямая инструкция проекта), и тогда — с
  комментарием под прототипом. Потерянные ранее абзацы (например,
  блок про «ВРЕМЯ у нас в запасе») — восстанавливаем из источника.
- 19.09.2026 — подзаголовок (лекторий/цикл и т.п.) и абзац об авторе
  из источника НЕ теряем: подзаголовок включаем в текст (обычно сразу
  после блока об авторе или в шапку), абзац об авторе переносим в текст
  описания, не боясь дублирования с KLBLOCK; имя автора — жирным
  курсивом, его описание — курсивом.
- 20.09.2026 — АДРЕС (улица, дом) в текст описания и его шапку НЕ
  вносим; его место — только формальное поле «Место». Низ описания
  (адрес, проезд, телефон и т.п.) пока не заполняем — как его составлять,
  обсудим отдельно.
- 20.09.2026 — источник может держать данные лекции в JSON-LD (schema.org):
  bio автора, цены, продолжительность и т.п. даже если на странице они не
  выводятся. По правилу «ничего не теряем» такие данные вносим в текст
  описания полностью (например, абзац об авторе из поля bio JSON-LD
  лекции Сурдина «Марс: зона возможной жизни»). Цена в шапку текста НЕ
  ставится — она остаётся в формальных полях (см. п. 5).
