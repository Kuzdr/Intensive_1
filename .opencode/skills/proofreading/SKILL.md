---
name: proofreading
description: "Корректорское оформление русских текстов проекта «Научный календарь»: буква «ё», неразбиваемые пробелы, кавычки, тире, сокращения, числовые интервалы и другие типографские правила. Применять при написании любых текстов сайта (описания событий, посты, страницы). Использовать по запросу «корректор», «правописание», «ё», «неразбиваемые пробелы», «типографика», «оформление текста»."
---

# Корректорские правила «Научного календаря»

Применяются к ЛЮБЫМ текстам проекта: описания событий (из источника или для
«Элементов»), посты в Телеграм, страницы сайта. Скилл обучаемый: правила
накапливаются по мере обсуждения с пользователем; спорные случаи фиксируются
здесь с примерами.

## 0. Обязательная самопроверка перед выводом

Правила этого скилла НЕ применяются «в процессе написания по памяти».
Перед показом ЛЮБОГО текста сайта (описание события, пост, страница)
готовый текст прогоняется по чек-листу ОТДЕЛЬНЫМ проходом, от начала
до конца, по каждому пункту. Порядок — как в списке ниже.

### Жёсткая процедура (а не «постараться быть внимательным»)

Самопроверка выполняется БУКВАЛЬНО и даёт ВИДИМЫЙ результат, который
показываем пользователю вместе с текстом:

1. Из готового текста выписать списком ВСЕ «ё» и ВСЕ неразбиваемые пробелы
   (каждый символ, в порядке появления в тексте).
2. По каждому пункту списка — почему он правильный или почему исправляем.
   Проходить строго по чек-листу ниже, против хода написания.
3. Если в тексте найден ЛЮБОЙ nbsp или ЛЮБАЯ «ё», которых там быть не
   должно — исправить и прогнать пункты 1–2 ЗАНОВО по уже исправленному
   тексту. Цикл повторять, пока проход не даст ноль исправлений.
4. Только когда исправлений ноль — выводить текст.
5. Проход оставляет ПИСЬМЕННЫЙ АРТЕФАКТ: в рабочий файл прототипа
   (рядом с текстом) отдельной секцией-аудитом выписать ВСЕ «ё» из текста
   и ВСЕ nbsp-места; напротив каждой «ё» — одно из ДОПУСТИМЫХ оснований
   по чек-листу (смыслоразличительная пара / важна для чтения / список
   исключений или собственное имя) или вердикт «убрать/заменить на «е»»;
   если основание не ясно — писать «спросить пользователя», а не
   «оставить». Проход, не оставивший вердиктов-артефакта, считается
   НЕ выполненным. Сам список-прогон пользователю не показываем (чтобы
   не шуметь), но артефакт хранится и доступен для проверки.

### Чек-лист (проходим по готовому тексту буквально, строка за строкой)

1. **«ё»** — найти все «ё» и проверить каждую: остаётся только если
   смыслоразличительна, важна для чтения или входит в список исключений.
   В остальных случаях исправляем: «пройдем», «ученый»/«ученые»,
   «еще», «идет», «вперед», «оказывает» и т.п.
   ПРОВЕРИТЬ И ОБРАТНУЮ СТОРОНУ: пройти по пополняемому списку слов
   из файла `tools/yo_words.txt` (см. п. 1 «ё» ниже) — нет ли в тексте
   слов без «ё» там, где «ё» должна стоять («на нем» → «на нём»,
   «вешенка» → «вёшенка» и т.п.).
2. **Неразбиваемые пробелы** — для каждого `&nbsp;`/`&#160;`/U+00A0
   проверить, что он на правильном месте:
   - после однобуквенных предлогов и союзов («в», «с», «к», «о», «у»,
     «и», «а») — ставим;
   - НЕ после «но», «не», НЕ после двухбуквенных предлогов («от», «на»,
     «по», «из», «за», «до», «для»);
   - НЕ внутри названий организаций и площадок («Прямая речь»,
     «офис YADRO» — обычные пробелы);
   - НЕ после тире — nbsp ТОЛЬКО перед «—» (в середине предложения),
     после тире — обычный пробел: «станции&nbsp;— от&nbsp;выбора…»,
     НЕ «—&nbsp;от&nbsp;выбора…»;
   - между числом и словом, числом и единицей, инициалами — ставим.
   Потом проверить и обратную сторону: проход по ОБЫЧНЫМ пробелам —
   нет ли среди них тех, где nbsp обязан стоять.
3. **Кавычки** — по тексту только «ёлочки», вложенные „лапки“,
   английские — “…”. Никаких «прямых» и кавычек-палок.
4. **Тире и дефис** — «—» в предложениях (и после него обычный пробел),
   «–» в интервалах времени и чисел («18:00–19:30», «30–60 минут»),
   дефис внутри составных слов.
5. **Числа и единицы** — целые до 10 словом, от 10 цифрами; деньги
   «4000 руб.» (без деления на разряды в тексте), «₽» не используем,
   «руб.» с точкой.
6. **Сокращения и согласование** — «12+», «им.», «МГУ», «т. е.»;
   перечитываем окончания, падежи, род.

ВАЖНО: если в тексте встретилось правило, не отражённое в скилле, или
спорный случай — зафиксировать его в «Истории правил» (см. конец скилла)
и спросить пользователя, прежде чем продолжать.

## 1. Буква «ё»

«ё» ставится ТОЛЬКО когда смыслоразличительна или важна для чтения:

- смыслоразличительные пары слов: «всё»/«все», «о чём»/«чем»,
  «нёбо»/«небо», «осёл»/«осел», «звёзды» (мн.)/«звезды» (род. ед.);
  НО: «звезд» (пары нет) — «ё» не ставим.
- фамилии, особенно иностранные: «Чюрлёнис», «Шрёдингер».
- слова, в которых часто делают ошибки, и иноязычные слова:
  «вёшенка», «Мнёвники», «флёр».

В остальных случаях «ё» НЕ ставим, в т.ч. в словах-инвариантах,
где «ё» не смыслоразличительна:
«ещё» (пишем «еще»), «учёный»/«учёные» (пишем «ученый»/«ученые»),
«идёт» (пишем «идет»), «вперёд» (пишем «вперед»), «потом» и т. п.

ОБЯЗАТЕЛЬНО проверяем и ОБРАТНУЮ сторону: где «ё» ДОЛЖНА стоять, но её
нет. Для этого есть пополняемый список слов — файл `tools/yo_words.txt`
(по одному слову в строке, категории разделены заголовками `# -- N --`):
- категория 1 — проверить ТОЛЬКО эту конкретную форму («все»/«всё»,
  «чем»/«чём», «нем»/«нём», «звезды»/«звёзды», «весны»/«вёсны»,
  «осел»/«осёл»); какое именно слово имелось в виду — решаем по контексту;
- категория 2 — проверить во всех формах («небо»/«нёбо», «Тема»/«Тёма»);
- категория 3 — в этой форме «ё» ставится ВСЕГДА;
- категория 4 — во всех формах «ё» ставится ВСЕГДА: «вешенка»→«вёшенка»,
  «Вешенская»→«Вёшенская», «Мневники»→«Мнёвники», «Чюрленис»→«Чюрлёнис»,
  «Шредингер»→«Шрёдингер».
Слово в списке с ЗАГЛАВНОЙ буквы означает: проверять, только если в
тексте оно написано с заглавной. Список пополняется — при каждом проходе
читаем его заново, а новое слово, замеченное в обсуждениях, добавляем в
файл (и в список исключений «ё», если это «всегда-ё»-слово).

ОТДЕЛЬНОЕ ПРАВИЛО: проверить ВСЕ имена собственные в тексте, содержащие
«е» — не должно ли там быть «ё» (фамилии, названия: Шрёдингер, Чюрлёнис,
Мнёвники, Вёшенская и т.п.). Это правило — поверх списка «yo_words.txt»:
список пополняемый, а упоминание любого имени собственного могло не
попасть в него.

## 2. Неразбиваемые пробелы

Длинные пробелы (`&#160;` / `&nbsp;` в HTML, U+00A0 в тексте) ставятся
внутри связок, которые нельзя разрывать при переносе строки:

- между числом и словом: «17 **сентября»**, «2000 **руб.»**, «XIX **века»**;
- между словом и числом: номер **17**, Пётр **I**;
- число принадлежит ОДНОМУ соседнему слову: либо слову ПОСЛЕ числа
  («1&nbsp;миллион», «29&nbsp;сентября»), либо слову ДО числа («Лекция&nbsp;1
  курса…»). Если число стоит между двумя словами и относится к левому, справа
  обычный пробел: «Лекция&nbsp;1 курса Павла…», НЕ «Лекция&nbsp;1&nbsp;курса».
  К обоим соседним словам сразу число относиться не может;
  НО: если слева от числа стоит ОДНОБУКВЕННЫЙ предлог/союз, его nbsp —
  обязательный (после «в»/«с»/…), а не привязка числа: «В&nbsp;2025&nbsp;году»
  корректно (число относится к «году», левый nbsp — после предлога);
- между инициалами и между инициалом и фамилией: «Н. **А.** Некрасов»;
- внутри сокращений с точкой («т. е.», «и т. п.»);
- между адресными сокращениями «г.», «д.», «ул.», «корп.», «стр.» и
  следующим словом/числом: «д.&nbsp;1», «г.&nbsp;Москва», «ул.&nbsp;Тверская»;
  НЕ «д.1» слитно;
 - перед знаком «—» (тире) в середине предложения: «станции&nbsp;— от&nbsp;выбора…»;
   НЕ после тире — там ВСЕГДА обычный пробел. Запоминаем как «nbsp ТОЛЬКО
   перед тире, после тире — обычный пробел, и точка».
   После тире nbsp НЕ ставим НИКОГДА, даже если дальше идёт однобуквенный
   предлог/союз:
   - неверно: «—&nbsp;от&nbsp;выбора…», «—&nbsp;с&nbsp;…»;
   - верно: «— от&nbsp;выбора…», «— с&nbsp;…» (nbsp после тире отсутствует).

Ставим после однобуквенных предлогов и союзов: «в», «с», «к», «о», «у»,
«и», «а» и т.п. — «и&nbsp;какой», «а&nbsp;иногда».

В НАЗВАНИЯХ организаций, площадок, лекториев внутри названия обычные
пробелы, nbsp ставим ТОЛЬКО в стандартных случаях (однобуквенные предлоги
и союзы, числа, инициалы):
- неправильно: «Центр&nbsp;Станислава&nbsp;Дробышевского», «Прямая&nbsp;речь»;
- правильно: «Центр Станислава Дробышевского», «Прямая речь».

Частные правила названий СИЛЬНЕЕ общих и применяются, даже если противоречат
им или не вытекают из них:
- «NAUKA&nbsp;0+» — неразбиваемый пробел между NAUKA и 0+ во всех текстах
  проекта (название фестиваля). НЕ «NAUKA 0+» и не «NAUKA&nbsp;&nbsp;0+».

НЕ ставим после:

- «но», «не»;
- двухбуквенных предлогов и союзов: «от», «на», «по», «из», «за», «до»;
- «значимых» однобуквенных слов, не являющихся предлогами/союзами
  (например, «я»).

В инициалах ставим разумно (после каждого: «Н.&nbsp;А.&nbsp;Некрасов»).
Остальные пробелы — обычные.

## 3. Кавычки

- Основные кавычки — «ёлочки»: «…».
- Вложенные — „лапки“: «Он сказал „да“».
- Вокруг английского текста — “English quotes”. 
- В HTML и коде допустимо `&laquo; … &raquo;` или сами символы.

## 4. Тире и дефис

- Длинное тире «—» (U+2014) — в предложениях (отделяет части), при
  значении «это», между числами как «от … до …» в тексте.
- Короткое тире «–» (U+2013) — в интервалах чисел и времени:
  «18:30–21:00», «30–60 минут», «5–7 слов».
- Дефис «-» — только внутри составных слов: «научно-популярный»,
  «кандидат технических наук».
- В HTML используем символы «—», «–» напрямую, а не сущности
  `&mdash;`/`&ndash;`.

## 5. Числа и единицы

- Целые числа до 10 словом, от 10 — цифрами (в тексте описаний).
- Деньги: «4000 руб.» без пробелов между разрядами цифр
  (для поста «10000»), в HTML на сайте допускается «40 000 руб.».
- Проценты: «10%» в тексте можно словом «десять процентов», цифрами «10%»
  (пробел между цифрой и знаком % не ставится).

## 6. Сокращения

- Допустимые: «им.» (имени), 
  «МГУ», «РАН», «ВДНХ», «т. п.», «т. е.».
- Не сокращаем случайные слова самовольно в основном тексте;
  в списках/шапках можно «ЧЧ:ММ» и т.п.
- «руб.» — с точкой; «₽» не используем.

## 7. Скобки и списки

- Скобки круглые — пояснения; «(от 4000 руб.)» оставляем.
- Порядок нумерации списков — как в источнике, если нет повода менять.

## 8. Предлоги и «в/во»

- Перед словами на труднопроизносимое начало: «**во** вторник»,
  «**во** дворце», «**в** четверг», «**в** среду» (зависит от начала слова).

## История правил (заполняется по мере обсуждения)

- 26.09.2026 — правило nbsp для ЧИСЛА между двумя словами: число
  принадлежит ОДНОМУ соседнему слову — либо слову ПОСЛЕ («1&nbsp;миллион»),
  либо слову ДО («Лекция&nbsp;1 курса…»). Со стороны слова ДО числа nbsp
  ставим, со стороны слова ПОСЛЕ — обычный пробел; к обоим сразу число
  относиться не может («Лекция&nbsp;1&nbsp;курса» — ошибка). Исключение:
  если слева от числа стоит ОДНОБУКВЕННЫЙ предлог/союз, его nbsp
  обязателен и не «привязывает» число («В&nbsp;2025&nbsp;году» корректно).
  Записано пользователем при разборе прототипа Усанова (TP-4206645);
  зеркало правила добавлено в `tools/check_prototype.py`
  (`p_nbsp_double_number`, тег 1d; исключение из 1a).

- 26.09.2026 — урок на «изобрёл»: глагол в прототипе Усанова (TP-4206645)
  был взят из источника с «ё», но по классификации символа (основание из
  трёх допустимых: пара / важна для чтения / список) он НЕ проходит —
  это нарицательное слово без пары («изобрел/изобрел»), не фамилия и
  не «всегда-ё». «ё» важна для чтения» — недостаточное основание само по
  себе (см. урок 25.09): для нарицательных слов действует список
  инвариантов. Аудит по готовому тексту вместо «оставить» дал «убрать»:
  «изобрел», «взлета», «остается», «недооцененным». Проверка «ё»
  при переносе текста из источника идёт по тем же правилам, что и для
  написанного текста.

- 25.09.2026 — урок: лишние «ё» («объединённая», «тёмная»,
  «Объединённого») проскочили не из-за «пропущенного» прохода, а из-за
  направления прохода: искали оправдание для УЖЕ написанной «ё» («похоже
  уместно», «официальное написание»), а не классифицировали слово по
  закрытому списку оснований. Теперь (п. 0, п. 5) вердикт по каждому
  символу: основание из трёх допустимых (пара / важна для чтения /
  список исключений или собственное имя), иначе — «убрать» или
  «спросить пользователя». Проход обязательно оставляет письменный
  артефакт-аудит; без него проход не засчитывается.

- 25.09.2026 — пополняемый список слов для ОБРАТНОЙ проверки «ё» (где «ё»
  должна стоять, но её нет) вынесен в отдельный файл `tools/yo_words.txt`;
  обе проверки и п. 0 (чек-лист п. 1), и п. 1 «ё» ссылаются на него.
  Урок 25.09.2026: «на нем» вместо «на нём» не было поймано ни одним из
  скиллов, потому что оба прохода проверяли только НАЛИЧИЕ «ё», а не её
  ОТСУТСТВИЕ в ожидаемом месте. Теперь обратная проверка обязательна:
  пройти по списку `tools/yo_words.txt` и по всем именам собственным
  с «е».

- 24.09.2026 — адресные сокращения «г.», «д.», «ул.», «корп.», «стр.»:
  nbsp между сокращением и следующим словом/числом («д.&nbsp;1»,
  «г.&nbsp;Москва»), НЕ «д.1» слитно. Формальные поля проверяются по тем
  же правилам nbsp, что и текст описания.

- 21.09.2026 — ПРОТОКОЛ ПРИМЕНЕНИЯ: чек-лист прогоняем по ГОТОВОМУ тексту
  ОТДЕЛЬНЫМ проходом, строка за строкой, ПОСЛЕ завершения написания —
  а не «в процессе письма по памяти». Перед выводом ЛЮБОГО текста
  обязательно находим и проверяем КАЖДЫЙ символ из чек-листа:
  каждую «ё», каждый nbsp (в обе стороны: и «лишние», и «недостающие»).
  Если найдено хоть одно исправление — прогоняем чек-лист ЗАНОВО
  по уже исправленному тексту.
  Повторяющиеся типовые ошибки (для запоминания):
  - лишний nbsp ПОСЛЕ тире, вместо обычного пробела:
    НЕ «—&nbsp;от&nbsp;выбора…», а «— от выбора…» (nbsp только ПЕРЕД «—»);
  - лишний nbsp ПОСЛЕ двухбуквенных предлогов: «от выбора», «для
    строительства», «до правил», «на площадке» — обычные пробелы;
  - лишняя «ё» в инвариантах: «еще», «идет», «ученый»/«ученые»;
  - непроставленный nbsp перед знаком «—» (между словом и тире).

- 19.09.2026 — полезно вести список слов, в которых часто ставят ё, но 
  делать этого не нужно:
  ее, нее, ученые, еще, изобрел, взлет, остается, недооцененным
